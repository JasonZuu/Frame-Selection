# Data Loader
dataloader_params:
  data_csv_path: "data/datasets/FF++/cbr/data.csv"
  num_workers: 4
  pin_memory: False

# Strategy
strategy_params:
  score_key: "random"
  strategy_key: "normal"
  score_img_shape: [64, 64]

# Evaluation
facetool_params:
  weights_path: "data/weights/dbface/dbface.pth"
  device: "cuda:0"
  face_shape: [224, 224]

model_params:
  key: "man"
  weights_path: "data/weights/teacher/man.pth"
  num_classes: 2
  device: &eval_model_device "cuda:0"

eval_params:
  key: "precision"
  device: *eval_model_device

# Running
running_params:
  group_size: 25
  frac: 0.04







